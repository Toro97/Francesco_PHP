<?php
// Scrivi uno script PHP che crea prende array di numeri interi, poi utilizza un ciclo foreach per
// stampare ogni numero moltiplicato per 2.

$vector=[1,2,3,4,5,6];

foreach ($vector as $el) {
    echo $el*2;
    echo "\n";
}